# Reviews & design notes

Code reviews with a concrete remediation sketch, one topic per file.

| # | Doc | Scope |
|---|-----|-------|
| 01 | [union-refactor-review.md](union-refactor-review.md) | Union + split refactor on `OperatorBuilder` (both landed; v1 split and local `Forward` deleted) — Termux lint caveats and the remaining split follow-ups |
| 02 | [2026-09-21-pre-commit-assessment.md](2026-09-21-pre-commit-assessment.md) | Pre-commit assessment of the `new-scheduler-tests` working tree: blockers 1–5 fixed (incl. restoring `Cloned`); remaining debug-span/`expect`/TODO triage and the staged `docs/notes` symlink policy concern |

Conventions mirror [`../overviews/README.md`](../overviews/README.md): link to code, don't
duplicate it; each file carries a `> **Last refreshed:**` header.

Conventions mirror [`../overviews/README.md`](../overviews/README.md): link to code, don't
duplicate it; each file carries a `> **Last refreshed:**` header.
