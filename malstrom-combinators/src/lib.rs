//! User-facing combinators, sinks and sources for Malstrom jobs (the
//! `StreamBuilder` extension surface), built on the kernel's operator API.

pub mod combinators;
pub mod keyed;
pub mod sinks;
pub mod sources;
